# Changelog

本项目遵循 Keep a Changelog 和语义化版本。

## [未发布]

### 新增

- 单实例守卫：重复启动应用时唤醒已运行实例的主窗口并退出，避免多实例争抢数据文件。
- 每日摘要通知时间可自定义（默认 18:00），在设置页「通知与隐私」中修改即时生效。

### 修复

- 统计分析页的周期选择在切换标签页后不再重置为「周」，改为保持上一次选择。

## [1.0.0] - 2026-08-19

### 新增

- 前台窗口事件监听、空闲检测、锁屏/休眠状态和唯一活动会话状态机。
- 可配置软件匹配、运行软件扫描、批量启停和软件级统计策略。
- 单主窗口以及概览、软件管理、统计分析、时间记录和设置页面。
- 日、周、月、年和全部时间统计，包含排行和柱状趋势。
- 原始会话补录、修改、删除、合并、标记空闲和 CSV 导出。
- JSON 原子持久化、损坏回退、ZIP 备份恢复和数据完整性检查。
- 系统托盘、开机启动、隐私模式、主题切换、连续使用提醒和每日摘要。
- 自包含单文件发布以及 22 个领域和服务层测试。
- Windows 快捷脚本 `build.bat`/`test.bat`/`clean.bat`/`publish.bat`/`ci.bat`。
- 应用专属图标（蓝底圆盘扇形），同时用于任务栏、主窗口、侧边栏和系统托盘。
- `tools/generate-icon.ps1` 可重现地生成多尺寸 `app.ico`，入口为 `sh manager.sh icon`。
- `tools/capture-ui.ps1` 启动应用、切换页面并截图，用于 UI 改动的实跑核对。
- 可缩放堆叠柱状图控件：滚轮以鼠标为锚点缩放、按住拖拽平移、双击复位；槽位过密时按整数倍自动合并。
- 图表图例：列出全部软件及其颜色和时长，悬停图例项或柱段时高亮同一软件、其余淡出。
- 柱段悬浮提示：展示所在时间区间、该区间内各软件的时长明细和合计。
- 软件配色槽位：8 个固定色相按软件自动分配，浅色与深色各一套，已通过色觉障碍分离度校验。
- 排行榜显示软件真实图标，取不到图标时才回退到首字母色块。
- 全局快捷键：系统级注册，按组合键呼出 / 隐藏主窗口，设置页可自定义。
- 统计分析「日」与「全部」视图改为按软件绘制柱条，每软件一根柱并按总时长降序。
- 统计分析「年」视图改为按周绘制柱条，横轴以周为刻度。

### 变更

- 默认统计口径由“进程正在运行”升级为“软件处于前台有效使用”。
- 概览「今日活动时间线」由甘特式活动段改为按分钟分箱的可缩放堆叠柱状图，横轴为时间，纵轴为分钟。
- 统计分析「每日使用时长」改用同一套可缩放柱状图，横轴按自然日补齐空白日，月视图不再切换成热力图。
- 概览「今日排行」和统计分析「软件排行」不再只显示前 5 项，改为完整列表加滚动。
- 历史软件条目原本共用同一个默认蓝，启动时自动按调色板槽位铺开并回写。
- 统计柱状图标题随周期切换为「每日 / 每周 / 各软件使用时长」。
- 概览页移除「添加当前软件」按钮，添加入口仍保留在系统托盘菜单。
- 左侧导航由 Unicode 符号改为 Segoe MDL2 Assets 图标字体，图标风格统一、基线对齐。
- 设置页所有选项改为修改即时生效，移除「保存设置」按钮。
- 全局快捷键设置改为「聚焦即捕捉」：点击输入框进入捕捉态（边框高亮并提示），直接按组合键自动替换；捕捉期间不响应呼出/隐藏，避免窗口被突然隐藏。
- 软件管理筛选栏的「全部分类」「全部状态」与搜索框一起靠左对齐。

### 修复

- 设置中的主题 Light/Dark 现在真正生效：调色板拆分为可整体替换的资源字典，控件样式改用 `DynamicResource`，选择后立即切换且无需重启。
- 深色主题下输入框、下拉项、表格行与单元格的前景色缺失问题一并修正。
- 暗色模式下 ComboBox 下拉、DatePicker 日历与 ToolTip 的文字仍为黑色，通过覆盖 `SystemColors` 画刷键修正；ComboBox 收起框改用显式模板，选中文字与背景真正跟随主题。
- 软件管理搜索框由占满整行改为固定 200px，避免过长。
- 时间记录页日历组件的日期数字未居中对齐，补 `Calendar`/`CalendarDayButton`/`CalendarButton` 样式显式居中并统一字体。

- 软件管理“扫描运行软件”选中进程后点击“使用选中项”，现在会正确打开“添加软件”编辑器并自动填入名称、进程名、路径和图标。
- 时间记录列表单元格内容改为垂直居中，行内操作按钮移出列表、置于列表下方，不再悬浮覆盖列表。
- 软件管理列表的操作按钮同样移出列表、置于列表下方，与时间记录页保持一致。
- 关闭窗口时按设置最小化到系统托盘的行为保持可用，并补充了回归验证。

## [0.1.0] - 2026-08-10

### 新增

- 初始化 .NET 8 WPF 主项目和 xUnit 测试项目。
- 引入 CommunityToolkit.Mvvm 8.3.2。
- 配置 WinForms NotifyIcon 支持和单文件发布基线。
- 初始化 AI 协作文档体系和统一 `manager.sh`。
- 纳入产品设计文档及四张界面设计图。
- 生成分阶段开发计划。
